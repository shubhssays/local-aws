# Server
PORT=4580
HOST=0.0.0.0

# LocalStack / AWS
AWS_ENDPOINT_URL=http://localhost:4566
AWS_DEFAULT_REGION=us-east-1

# LocalStack services to enable (see docker-compose.yml)
# SERVICES=s3,sqs,dynamodb,sns,secretsmanager,ssm,lambda,logs,events,stepfunctions
